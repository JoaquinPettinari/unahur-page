---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";
import TitleDivider from "../components/TitleDivider.astro";

interface Props {
  title: string;
  epigraph: string;
  img: ImageMetadata;
  reverse?: boolean;
  buttonLabel: string;
  linkButton: string;
}

const { img, epigraph, title, buttonLabel, linkButton, reverse } = Astro.props;
---

<article
  class:list={"w-full grid grid-cols-1 items-center justify-items-center lg:grid-cols-2"}
  id={"#events"}
>
  <div class:list={[{ "order-last": reverse }]}>
    <Image src={img} alt="Image of event" />
  </div>
  <div class="w-full lg:w-5/6">
    <p class="primary-color text-xl font-bold uppercase mb-4">{epigraph}</p>
    <p class="text-gray-800 text-5xl font-bold">{title}</p>
    <TitleDivider />
    <div class="mt-4 mb-8">
      <slot name="body" />
    </div>
    <slot name="extra-body" />
    <a
      class="primary-color border py-2 px-5 rounded-3xl border-[var(--primaryColor);]"
      href={linkButton}
      target="_blank">{buttonLabel}</a
    >
  </div>
</article>
